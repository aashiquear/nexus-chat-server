# Shared network so standalone MCP servers can also connect.
# Create it once with:  docker network create nexus-net
# Standalone containers join with:  docker run --network nexus-net ...
networks:
  nexus-net:
    name: nexus-net
    driver: bridge

services:
  nexus-chat:
    build: .
    container_name: nexus-chat-server
    networks:
      - nexus-net
    ports:
      - "8000:8000"
    environment:
      - NEXUS_ADMIN_PASSWORD=${NEXUS_ADMIN_PASSWORD:?err}
      - NEXUS_JWT_SECRET=${NEXUS_JWT_SECRET:-change-me-in-production}
      - OLLAMA_BASE_URL=${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      - OLLAMA_API_KEY=${OLLAMA_API_KEY:-}
      - ANONYMIZED_TELEMETRY=False
    volumes:
      # Mount config for easy editing without rebuild
      - ./config:/app/config:ro
      # Persist data (conversations, uploads, vector store)
      - nexus-data:/app/data
    extra_hosts:
      # Allow container to reach Ollama on the host machine
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3

  nexus-sandbox:
    build: ./sandbox
    container_name: nexus-sandbox
    networks:
      - nexus-net
    # Bind to localhost only — the sandbox is reachable from the
    # nexus-chat container over the docker network, not the host.
    expose:
      - "8500"
    environment:
      - SANDBOX_TIMEOUT=60
    mem_limit: 512m
    cpus: '1.0'
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8500/health')"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

  # ── MCP Servers (optional, enable with: docker compose --profile mcp up) ──

  mcp-database:
    build: ./examples/mcp-database-server
    networks:
      - nexus-net
    profiles:
      - mcp
    ports:
      - "8100:8100"
    volumes:
      - mcp-db-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8100/health')"]
      interval: 15s
      timeout: 3s
      start_period: 5s
      retries: 3

volumes:
  nexus-data:
  mcp-db-data:
